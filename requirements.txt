# APEX shared Python dependencies
# Pin minor versions for reproducibility; update deliberately.

# Async + networking
httpx>=0.27,<1.0
anyio>=4.0,<5.0
websockets>=12.0,<14.0

# Kafka
confluent-kafka>=2.3,<3.0

# Redis
redis>=5.0,<6.0

# Dataframe + numerical
numpy>=1.26,<2.0
pandas>=2.1,<3.0

# YAML config
pyyaml>=6.0,<7.0

# Structured logging
structlog>=24.0,<26.0

# ML experiment tracking
mlflow>=3.0,<4.0

# HTTP server (signal_engine API, if needed)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
prometheus-client>=0.19.0

# Type annotations
typing_extensions>=4.10,<5.0

# Phase 0: isotonic calibration + async DB for decision records
scikit-learn>=1.3,<2.0
asyncpg>=0.29,<1.0
